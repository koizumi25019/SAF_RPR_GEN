//------------------------------------------------------------------------
//File name : StandardHead.h
//Date : 2011/06/14
//Designer : H.Yamazaki
//Ver : 0.01
//------------------------------------------------------------------------
#include "../../Netlist/s_netlist.h"

//========================================================================
// 定義
//========================================================================

//========================================================================
// プロトタイプ宣言
//========================================================================

//========================================================================
// 構造体
//========================================================================
//---------------------------------------------------
//解析用CPIリスト構造体
//---------------------------------------------------
typedef struct _cpi_list_ {	

	int		n;					//インデックス
	char	ffc_name_ins[1000];	//FFCインスタンス名
	char	ff_name[1000];		//オリジナルFF名
	int		n_corn_ppo;			//オリジナルFF(FFC)から到達可能なPPO数
	int		n_corn_ppi;			//オリジナルFF(FFC)から到達可能な(P)PI数
	int		n_corn_out_net;		//オリジナルFF(FFC)から到達可能な出力側の信号線数
	int		n_corn_in_net;		//オリジナルFF(FFC)から到達可能な入力側の信号線数
	S_NLIST	*net;				//オリジナルFFネットリストへのポインタ

}CPI_LIST;

//========================================================================
// 外部変数
//========================================================================
CPI_LIST *cpi_list;