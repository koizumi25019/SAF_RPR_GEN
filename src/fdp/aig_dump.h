#pragma once
//-------------------------------------------------------------------------------------------------------------
//	aig_dump.h : 検証(env AIG_DUMP=path): 対象故障の検出回路（正常 ∧ 故障コーン ∧
//	PO差分の OR = 出力）を単一出力 AIGER (ASCII .aag) として出力する。
//	AllSAT-CT ツール（HALL 等）と同一インスタンスで比較実験するため。
//	DUMP_CNF と同様、最初の対象故障を出力して即終了する（単一故障 flist で回す前提）。
//-------------------------------------------------------------------------------------------------------------
#include "./read.h"

/* 故障 f の検出回路を path に .aag で書き出す。成功で true */
bool AIG_Dump(const char* path, FNODE* f);
