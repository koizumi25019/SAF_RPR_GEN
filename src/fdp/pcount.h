#pragma once
//-------------------------------------------------------------------------------------------------------------
//	pcount.h : PODEM 型入力空間探索による厳密数え上げ（env PCOUNT=1）
//	打ち切り故障の検出パターン数 Vi を、PI への部分割り当てを深めながら
//	3値故障シミュレーションで「全拡張が検出/非検出」を早期確定し、
//	等価な回路状態のメモ化で共有して厳密に数える。
//	外部モデルカウンタ・ソルバ改造・回路構造からの BDD 直接合成は使わない
//	（探索は ATPG(PODEM) 流のトップダウン入力割り当て、圧縮はメモ化のみ）。
//	評価は verification/pcount/SUMMARY.md。
//-------------------------------------------------------------------------------------------------------------
#include "./read.h"

/* 故障 f の検出パターン数（ミンターム数/2^n_pi が fdp）を10進文字列で返す。
   malloc 済み・呼び出し側で free。探索ノード数が PCOUNT_MAXNODES を超えたら
   NULL（従来どおり capped 扱いにする）。 */
char* PC_ExactCountStr(FNODE* f);
